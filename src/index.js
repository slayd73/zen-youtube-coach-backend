// ======================================================// 🚀 Zen YouTube Coach Pro — Backend Entry (Ultra 2025)// Groq-first + OpenAI fallback// Endpoints completi per Dashboard 5173// ======================================================import express from "express";import cors from "cors";import dotenv from "dotenv";// ROUTES COREimport healthRouter from "./routes/health.js";import transcriptRouter from "./routes/transcript.js";import analyzeTranscriptRouter from "./routes/analyze-transcript.js";import analyzeRouter from "./routes/analyze.js";import analyzeVideoRouter from "./routes/analyze-video.js";import analyzerProRouter from "./routes/analyzer-pro.js";import compareTranscriptRouter from "./routes/compare-transcript.js";import compareVideosRouter from "./routes/compare-videos.js";import trendScanRouter from "./routes/trend-scan.js"; // ⭐ NUOVOdotenv.config();const app = express();const PORT = process.env.PORT || 4000;// ------------------------// Middlewares// ------------------------app.use(  cors({    origin: "*",    methods: ["GET", "POST", "PUT", "DELETE"],    allowedHeaders: ["Content-Type", "Authorization"]  }));app.use(express.json({ limit: "3mb" }));// ------------------------// Root endpoint// ------------------------app.get("/", (req, res) => {  res.json({    success: true,    message: "🔥 Zen YouTube Coach Pro — Backend attivo",    version: "Ultra-Premium 2025",    endpoints: [      "/health",      "/transcript",      "/analyze-transcript",      "/analyze",      "/analyze-video",      "/analyzer-pro",      "/compare-transcript",      "/compare-videos",      "/trend-scan" // ⭐ AGGIUNTO    ]  });});// ------------------------// Routes// ------------------------app.use("/health", healthRouter);app.use("/transcript", transcriptRouter);app.use("/analyze-transcript", analyzeTranscriptRouter);app.use("/analyze", analyzeRouter);app.use("/analyze-video", analyzeVideoRouter);app.use("/analyzer-pro", analyzerProRouter);app.use("/compare-transcript", compareTranscriptRouter);app.use("/compare-videos", compareVideosRouter);app.use("/trend-scan", trendScanRouter); // ⭐ AGGIUNTO UFFICIALMENTE// ------------------------// Avvio server// ------------------------app.listen(PORT, () => {  console.log("============================================");  console.log("🔥 Zen YouTube Coach Pro — Backend avviato");  console.log(`🌐 Server attivo su: http://localhost:${PORT}`);  console.log("============================================");});