import express from "express";import cors from "cors";import dotenv from "dotenv";dotenv.config();const app = express();app.use(express.json({ limit: "50mb" }));app.use(cors());// ====== IMPORT ROUTES ======import transcriptRoute from "./routes/transcript.js";import analyzeRoute from "./routes/analyze-transcript.js";import compareRoute from "./routes/compare-videos.js";import trendRoute from "./routes/trend-deepsearch.js";import healthRoute from "./routes/health.js";// ====== USE ROUTES ======app.use("/transcript", transcriptRoute);app.use("/analyze", analyzeRoute);app.use("/compare-videos", compareRoute);app.use("/trend-deepsearch", trendRoute);app.use("/healthz", healthRoute);// ====== PORT FIX PER RENDER ======const PORT = process.env.PORT || 4000;   // Render fornisce automaticamente PORTconst HOST = "0.0.0.0";                 // Obbligatorio su Renderapp.listen(PORT, HOST, () => {  console.log("============================================");  console.log("🔥 Zen YouTube Coach Pro — Backend avviato");  console.log(`🌐 Server attivo su: http://localhost:${PORT}`);  console.log("============================================");});